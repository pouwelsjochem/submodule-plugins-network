-------------------------------------------------------------------------------
-- New network.request() / network.cancel() test suite
-------------------------------------------------------------------------------

-- TODO: Remove this once we this is built into the simulator


local platformName = system.getInfo( "platformName" )

